package com.honeybadger.demowebapp.dbops.ui;

public record StudentUserOutput(String first_name,

                                String last_name,
                                String email,
                                String school_name) {
}
